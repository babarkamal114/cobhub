import { AvatarProps, ButtonProps, Color, Shape, Size, Variant } from "./components";
import { Sizes } from "./theme";

export type { 
    AvatarProps,
    ButtonProps,
    Color, 
    Shape,
    Size,
    Sizes,
    Variant,
}